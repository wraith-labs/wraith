kind: pipeline
type: docker
name: code quality and build
steps:
  - name: lint
    image: golangci/golangci-lint
    commands:
      - golangci-lint run -v ./wraith

  - name: build
    image: docker.io/golang:1.16-alpine
    commands:
      - apk add --no-cache build-base
      - go build -v -trimpath ./wraith